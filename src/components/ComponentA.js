function ComponentA() {
  return (
    <div>
      <h1>this is ComponentA</h1>
    </div>
  );
}

export default ComponentA; //default export

//before import we as to export we have 2 ways
//default export-in a file we have only 1 default export
//named export-we have n number of named export in a file
